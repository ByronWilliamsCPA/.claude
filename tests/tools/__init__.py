"""Tool-level tests for Claude Code Configuration.

Tests for standalone tools and scripts (e.g. catalog schema validation,
JSON integrity checks). These tests target tooling outputs rather than
application logic.

Mark tests with @pytest.mark.unit decorator for organization.
"""

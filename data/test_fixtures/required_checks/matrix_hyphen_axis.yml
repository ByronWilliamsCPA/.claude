name: Tests
on: [push]
jobs:
  test:
    name: Test (Python ${{ matrix.python-version }})
    strategy:
      matrix:
        python-version: ['3.11', '3.12']
    runs-on: ubuntu-latest
    steps:
      - run: echo hi

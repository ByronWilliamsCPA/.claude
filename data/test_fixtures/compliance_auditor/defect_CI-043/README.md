# Control Fixture

Clean baseline repo for the compliance auditor regression suite. Passes every
critical check that the suite covers. Each `defect_*` fixture is a copy of this
directory with exactly one element removed or corrupted.

Do not add unrelated files; keep the fixture minimal so each defect is easy to
reason about.

"""Compliance master-log tooling: shared helpers and the Markdown renderer.

The append and reconcile entrypoints still live under ``scripts/`` and move
into this package in a later migration step (ARCH-01 PR1b).
"""
